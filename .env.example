# ==== Gmail SMTP settings ====
SMTP_HOST=smtp.gmail.com
# Use 465 for SSL (recommended). If you prefer STARTTLS, set 587 and SMTP_USE_SSL=false
SMTP_PORT=465
SMTP_USE_SSL=true

# Your full Gmail address and the 16-character "App Password" you generated in Google Account > Security.
SMTP_USERNAME=noreply@example.com
SMTP_PASSWORD=REPLACE_WITH_YOUR_APP_PASSWORD

# From display name and default subject
FROM_NAME=Your Company Name
SUBJECT=Your Campaign Subject

# Path to your HTML email template (Jinja2-compatible).
# Tip: keep your working templates in ./templates and only commit example.html
TEMPLATE_PATH=./templates/example.html

# Optional reply-to address (e.g., a support inbox); leave empty to omit
REPLY_TO=

# Optional global rate limit (emails per minute). You can also set this via --rate at runtime
RATE_PER_MIN=12
